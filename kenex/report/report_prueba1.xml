<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data>

        <record id="action_report_plantilla1" model="ir.actions.report">
            <field name="name">Plantlla 1</field>
            <field name="model">kenex</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name"></field> 
            <field name="report_file"></field> 
            <field name="binding_model_id" ref="model_kenex"/> <!-- el ref hace referencia al modelo-->
            <field name="binding_type">report</field> 
        </record>
    
        <template id= "reporte_plantilla1">
        </template>

    <!---
        <field name="print_report_name">('Factura - %s' %(object.name)%)</field>
            <field name="binding_model_id" ref="model_kenex"/>  el ref hace referencia al modelo
            <field name="binding_type">report</field> 
        <table>
            <thead>
            </thead>
            <tbody>
            </tbody>
        </table>
    -->
    
    </data>
</odoo>